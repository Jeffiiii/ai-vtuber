# Local config (may hold API keys later)
config.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Runtime artifacts
*.log
memory_store/
recordings/
output/
.vts_token

# Training / serving artifacts
posttrain_results/
.venv-train/
# large model weights (keep the folder via .gitkeep)
models/*.gguf
*.safetensors
*.bin
