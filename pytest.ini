[pytest]
# Only collect the fast, dependency-free unit tests by default.
# The training/eval scripts under scripts/posttrain/ require torch/peft/transformers
# and are meant to run on your GPU box (WSL2), not in plain `pytest`.
testpaths = tests
addopts = -q
