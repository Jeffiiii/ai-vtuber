# ===========================================================================
# ai-vtuber — Stage 2 voice extras (TTS + STT)
# ===========================================================================
# Install when you start the voice stage:  pip install -r requirements-voice.txt
# (In China, your TUNA pip mirror covers most; XTTS downloads its model on first use.)
# The text app does NOT need any of this.
# ===========================================================================

# --- TTS (voice out) ---
TTS>=0.22                # Coqui XTTS v2 — local, offline, EN/ZH; DEFAULT voice backend
transformers<4.41        # Coqui TTS uses the old generation API (BeamSearchScorer);
                         # transformers >=4.41 removed it. This venv talks to the LLM over
                         # HTTP (local_http), so it does NOT need a newer transformers.
playsound==1.2.2         # plays the wav/mp3 output (1.2.2 is the stable release)
edge-tts>=6.1           # optional alternative (free, online) — tts_backend: "edge"

# --- STT (the ears) — needed for --listen ---
faster-whisper>=1.0
sounddevice>=0.4.6      # microphone capture
soundfile>=0.12         # write WAV for the recognizer
numpy>=1.26
